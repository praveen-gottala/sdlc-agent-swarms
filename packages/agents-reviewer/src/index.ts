/**
 * @module @agentforge/agents-reviewer
 *
 * Reviewer pipeline — fourth spine stage (vision Layer 9).
 * Deterministic gates first, LLM review second. Emits ReviewResult
 * with disposition enum for caller-driven bounded retry.
 */
