/**
 * Re-export from @agentforge/core — canonical location is packages/core/src/design/.
 * This file exists for backward compatibility with existing CLI-internal imports.
 */
export {
  DEFAULT_LAYOUT_TOKENS,
  SHARED_LAYOUT,
  DEFAULT_OPACITY,
  DEFAULT_MOTION,
  DEFAULT_STATE,
  DEFAULT_ELEVATION,
  DEFAULT_TYPOGRAPHY_SCALE,
  DEFAULT_PREVIEW,
} from '@agentforge/core';
export type { PreviewData } from '@agentforge/core';
