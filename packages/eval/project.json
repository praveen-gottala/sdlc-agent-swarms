{
  "name": "@agentforge/eval",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "inputs": [
        "{projectRoot}/src/**/*.ts",
        "!{projectRoot}/src/**/*.test.ts",
        "{projectRoot}/src/scenarios/**/*.yaml",
        "{projectRoot}/tsconfig.lib.json",
        "{projectRoot}/tsconfig.json"
      ],
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "commands": [
          "tsc --build tsconfig.lib.json",
          "mkdir -p dist/scenarios && cp src/scenarios/*.yaml dist/scenarios/"
        ],
        "cwd": "packages/eval",
        "parallel": false
      },
      "dependsOn": ["build-deps"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/eval",
        "command": "jest"
      },
      "configurations": {}
    }
  }
}
