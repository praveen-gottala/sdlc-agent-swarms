/**
 * Re-exports generateCssVariables from the shared browser module.
 * The implementation lives one level up so Jest tests can also import it.
 */
export { generateCssVariables } from '@shared/renderer/browser/generate-css-variables';
