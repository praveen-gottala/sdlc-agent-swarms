# Architecture

How the system is built — system design, data flows, component systems,
abstractions, and agent contracts. For product requirements (what to build),
see `../specs/`. For operational guides (how to use), see `../guides/`.
