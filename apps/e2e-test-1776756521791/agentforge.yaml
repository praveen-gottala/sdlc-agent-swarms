version: "1.0"
project:
  name: E2E Test 1776756521791
  description: A task management app with a dashboard, task list, task detail
    view, a notifications drawer that slides in from the right, and a
    confirmation dialog for deleting tasks.
  platforms:
    - web
stack:
  frontend: react
  backend: node
  database: postgresql
  styling: tailwind
budget:
  per_task_max_usd: 2
  per_phase_max_usd: 25
  monthly_max_usd: 200
