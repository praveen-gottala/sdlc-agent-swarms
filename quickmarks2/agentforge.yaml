version: "1.0"
project:
  name: QuickMarks2
  description: A personal bookmark manager where users save, tag, and filter links.
  platforms:
    - web
stack:
  frontend: react
  backend: node
  database: postgresql
  styling: tailwind
budget:
  per_task_max_usd: 2
  per_phase_max_usd: 25
  monthly_max_usd: 200
